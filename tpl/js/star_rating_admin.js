/**
 * @file   modules/star_rating_config/js/star_rating_admin.js
 * @author 1Sam (csh@korea.com)
 * @brief  star_rating_config 모듈의 관리자용 javascript
 **/

jQuery(function () {

  jQuery('[data-toggle="tooltip"]').tooltip();
  //jQuery(".toggle_checkbox").bootstrapSwitch();
  //jQuery.fn.bootstrapSwitch.defaults.size = 'mini';

/*jQuery('select').on('change', function() {
	var numberPattern = /\d+/g;
	var id = (this.name).match( numberPattern );

		jQuery("select[name='star_skin_view[id]]'").value('default');
  		alert( this.value +'//'+id );
})*/


});
